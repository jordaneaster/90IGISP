# Disable cache entirely during Docker builds
cache=false
prefer-offline=true
no-update-notifier=true

# Force npm to use a different cache location if cache is used
cache-min=9999999
cache-max=0

# Avoid filesystem locking issues
force=true
