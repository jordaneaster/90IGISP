# Completely disable npm cache features
cache=false

# Disable non-essential features
fund=false
audit=false

# Avoid filesystem locking issues
unsafe-perm=true

# Performance settings
loglevel=error

# Compatibility settings
legacy-peer-deps=true
# Add legacy-peer-deps for compatibility
legacy-peer-deps=true
