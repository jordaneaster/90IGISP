# Disable cache entirely during Docker builds
cache=false
fund=false
audit=false
update-notifier=false

# Avoid filesystem locking issues
force=true
unsafe-perm=true
no-optional=true

# Performance settings
loglevel=error
fetch-retries=5
fetch-retry-maxtimeout=60000
