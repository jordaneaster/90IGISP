# Completely disable cache
cache=false
# Skip problematic checks
audit=false
fund=false
# Avoid networking issues
fetch-retries=5
fetch-retry-factor=2
fetch-timeout=60000
# Bypass permission issues
unsafe-perm=true
# Allow legacy dependencies
legacy-peer-deps=true
# Use a simple installation approach
save-exact=true
